export enum TransactionStatus {
    PENDING = 'PENDING',
    AWAITING_CONFIRMATION = 'AWAITING_CONFIRMATION',
    CONFIRMED = 'CONFIRMED',
    COMPLETED = 'COMPLETED',
    FAILED = 'FAILED',
    CANCELED = 'CANCELED',
}
