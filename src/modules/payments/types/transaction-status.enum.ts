export enum TransactionStatus {
    PENDING = 'PENDING',
    PARTIAL = 'PARTIAL',
    AWAITING_CONFIRMATION = 'AWAITING_CONFIRMATION',
    CONFIRMED = 'CONFIRMED',
    COMPLETED = 'COMPLETED',
    FAILED = 'FAILED',
    CANCELED = 'CANCELED',
}
